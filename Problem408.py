class Solution:
    def validWordAbbreviation(self, word: str, abbr: str) -> bool:
        # iterating through the command to get the actual command
        wholeWord = []
        i = 0
        while i < len(abbr):
            i += 1

        return wholeWord